export enum PetitionStatus
{
    NO_RESOLUTION = "nores",
    IN_PROGRESS = "progress",
    TERMINATED = "terminated",
    OVERDUE = "overdue"
}

export enum ResolutionStatus
{
    IN_PROGRESS = "progress",
    TERMINATED = "terminated",
    OVERDUE = "overdue"
}